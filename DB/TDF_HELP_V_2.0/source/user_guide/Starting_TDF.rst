Starting TDF
============

This section describes the typical steps to start TDF and open the main editors from a user’s workstation. It assumes that TDF and all prerequisites (TDF Editor) are already configured.
