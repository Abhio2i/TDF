#include "math_utils.h"
#include <cmath>

// Implementations are inline; this file can be empty or contain non‑template functions if needed.
// For now, it's just a placeholder because the functions are in header.